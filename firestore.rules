rules_version = '2';
/* 前端一律不直接碰 Firestore：所有讀寫都經過 Cloud Functions，
   130 道檢查留在後端，跟原本 Apps Script 版一模一樣。 */
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} { allow read, write: if false; }
  }
}
