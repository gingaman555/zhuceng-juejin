# 逐層掘進 · 守關生物（BOSS）

給 Claude Code 的改動規格。目標：把「採齊 → 送關卡 → 老師審」這個斷點，變成一場**看得到對手、也看得到自己拿什麼去打**的遭遇。

**範圍：前四層有 BOSS。第五層沒有。**

---

## 一、最重要的原則

**BOSS 不是新規則，是既有規則的戲劇化。** 規則一條都不改，只是換一套敘事講同一件事。

| 現在的規則 | 換成 BOSS 的說法 |
|---|---|
| 這一層的礦脈要開完、也要採完 | 採齊那一刻，武器才鑄得出來。少一塊，你連牠面前都走不到 |
| 送審三格 | 向老師提出申請——申請鑄造這一層的道具 |
| 老師審核通過 | 擊退。牠讓開路 |
| 老師退回 | 牠擋下來了。**退件理由就是牠說的話** |
| 通過拿道具與寶物 | 從牠身上取下的東西 |

**不要加入任何跟老師判斷無關的勝負機制**——沒有骰子、沒有戰鬥數值、沒有 HP 公式。
唯一決定勝負的仍然是老師寫的那段合格考量。這是作品的主旨，不能被遊戲性稀釋。

---

## 二、礦石鑄成武器，武器打 BOSS

這是這次改動的核心。**道具不再只是「下一層的通行證」——它是你用這一層採到的礦石鑄出來的武器，而且正好剋這一層的難處。**

一層之內的完整順序：

```
採礦石          老師逐項確認，一塊一塊進你的背包
   ↓
採齊            「這些礦石可以鑄成 ○○」浮出來——但你還鑄不了
   ↓
提出申請        送關卡三格＝跟他說明：你走過的路 ／ 你們的變化 ／ 接下來要做什麼
   ↓
他准了          礦石鑄成道具 → 用它擊退 BOSS → 取下寶物 → 下一層
他擋下          BOSS 擋住路。武器還不成立——三格重寫，重送次數不限
```

**礦石不再只是收集品，它是材料。**

| 層 | 採幾塊 | 鑄成 | 這一層的難處 | 為什麼剋得住 | BOSS |
|---|---|---|---|---|---|
| L1 微光荒原 | 6 塊 | **礦脈羅盤** | 不知道從哪裡開始挖 | 辨認方向真假——這條路通不通、值不值得追 | **霧面者** |
| L2 水晶迴廊 | 6 塊 | **地層切割器** | 採不完，不知道什麼時候該收 | 切開擋路的東西——一刀斬斷長不完的那些 | **千晶母** |
| L3 迴聲迷宮 | 5 塊 | **壓力量規** | 你以為知道的其實不知道 | 偵測應力——量出來的，不是聽來的 | **擬聲者** |
| L4 熔火深淵 | 5 塊 | **熱流導航儀** | 時間在漲，必須取捨 | 在無規律中找路——壓力下重新排序 | **漲潮者** |
| L5 星核聖殿 | 1 塊 | **核心共振器** | 他沒有給你清單 | 判斷何時該自己扛、何時該開口 | **（沒有）** |

> 這五組對應**本來就在設計包裡**（每一層的 `hard` 與 `toolFunc` 一直是配對的），這次只是把它變成看得見的因果。

### 第五層為什麼沒有 BOSS

第五層只有一塊礦石（完成之光），而且**是學生自己命名的**。鑄出來的核心共振器「不顯示任何讀數，你得自己聽它的音高」。

沒有東西擋你——**擋你的是你自己。** 這一點要在第五層的送審頁明講，那個空缺就是第五層的難處本身。

### 對應到程式

```js
/* 這一層的礦石鑄成這一層的道具，用來打這一層的 BOSS */
function forgeOf(L, layer) {
  var lay = L.LAYERS[layer - 1] || {};
  var st  = layerState(L, layer);
  return {
    name:   lay.tool,
    func:   lay.toolFunc,
    sprite: layerSprites(L, layer).tool,
    ready:  st.complete,                 // 採齊了才鑄得出來
    from:   st.veinN + ' 塊礦石',
    got:    st.gotN,
    forged: st.cleared                   // 老師准了才真的鑄成
  };
}
```

## 三、四隻生物

每一隻是那一層 `LAYERS[n].hard` 的化身——**真正的專案管理失敗模式**。

### L1 · 微光荒原 — 霧面者　`#C9A227`

- **難處**：難的不是挖不動，是不知道從哪裡開始挖。
- **失敗模式**：範圍未定義。沒定義之前，任何方向都一樣模糊。
- **外型**：比人高一截的細長身影，**沒有臉**——頭的位置是空的，你看不出牠朝哪邊。腳邊有霧。牠不動，也不攻擊。
- **醒來**：霧散開，才看得出牠一直站在同一個地方。
- 未採齊：「你連我在哪都說不出來。」
- 採齊：「現在你看得到我了。說說看你走過哪裡。」
- 被擋下：「牠沒有動。是你還沒說清楚。」
- **你用什麼打牠**：**礦脈羅盤**（這一層六塊礦石鑄的）。牠讓你看不出方向，羅盤辨認方向真假。

### L2 · 水晶迴廊 — 千晶母　`#5FA8C7`

- **難處**：難的是採不完，不知道什麼時候該收。
- **失敗模式**：範圍蔓延（scope creep）。挖越多長越多。
- **外型**：中央一顆晶核，四周不斷長出新的晶簇——**周圍碎晶持續增生**，看起來永遠採不完。
- **醒來**：外圍碎晶停止增生，只剩中央那一顆。
- 未採齊：「還有喔。這裡永遠還有。」
- 採齊：「你停手了。那才是難的部分。」
- 被擋下：「你還在挑。挑到什麼時候？」
- **你用什麼打牠**：**地層切割器**（這一層六塊礦石鑄的）。牠長不完，切割器一刀斬斷。

### L3 · 迴聲迷宮 — 擬聲者　`#8C7BC7`

- **難處**：難的是你以為知道的其實不知道。
- **失敗模式**：未驗證的假設。你聽到的是自己的回音。
- **外型**：一個實體 ＋ 兩個錯位殘影，形狀跟挖礦小人一樣——**牠在模仿你**。
- **醒來**：殘影收攏成一個，你才分得出哪個是牠。
- 未採齊：「你剛剛說的話，是你查過的，還是你以為的？」
- 採齊：「這次的聲音是你自己的了。」
- 被擋下：「你又把我的話當成你的答案了。」
- **你用什麼打牠**：**壓力量規**（這一層五塊礦石鑄的）。牠給你回音，量規給你量出來的數字。

### L4 · 熔火深淵 — 漲潮者　`#D9603F`

- **難處**：難的是時間在漲，必須取捨。
- **失敗模式**：時程壓縮。拖越久，能做的越少。
- **外型**：熔岩構成的身軀，**下半身是不斷上漲的岩漿**。
- **醒來**：岩漿退到腰部，牠整個站起來。
- 未採齊：「你還在挑要先做哪一個。時間不等。」
- 採齊：「你捨掉了一些東西。那就是取捨。」
- 被擋下：「你什麼都想留。那就什麼都做不完。」
- **你用什麼打牠**：**熱流導航儀**（這一層五塊礦石鑄的）。牠讓時間漲上來，導航儀在壓力下重新排序。

---

## 三·五、故事已經先鋪好了（不用再寫）

開頭的故事**已經改完並上線**，Claude Code 不需要動它——照著它講的做就好：

| 位置 | 內容 |
|---|---|
| 學生 · 開頭第四頁「而且路上不是空的」 | 每一層深處都蹲著一隻東西，長得就是那一層最難的那件事；第一層那隻沒有臉。插圖 `art:'beast'` |
| 學生 · 開頭第五頁「斗篷人手上有能打倒牠的東西」 | 他要礦石，一塊都不能少；採齊 → 提出申請 → 他點頭 → 鑄成道具 → 牠讓開。插圖 `art:'trade'` |
| 老師 · 「作品的故事 · 三」 | 同一件事從老師那一側說：你這邊沒有任何額外動作，你寫的判斷理由在他們那邊就是牠說的話 |

兩張插圖是 320×150 的 inline SVG，在 `patch-tpl.js` 的 patch 27；文案與頁面在 `gas/Live.html` 的 `P.storyPages` 覆寫。

**所以 BOSS 對學生來說不是突然冒出來的。** 第一次看到 S-02 那一列時，他們已經被告知過牠會在那裡。

---
## 四、出現在哪裡

### 1. S-02 任務清單頂端 — 常駐一列

放在採集進度上方：

```
┌────────────────────────────────────────────────┐
│ [BOSS 48px]  L2 · 千晶母                        │
│              「還有喔。這裡永遠還有。」            │
│                                                │
│  護甲 ●●●○○○  3 / 6 已剝落                      │
│  可以鑄成  [切割器圖] 地層切割器（還差 3 塊）      │
└────────────────────────────────────────────────┘
```

- 護甲格數＝那一層的礦脈塊數，採一塊亮一格。
- **未採齊**：BOSS 套 `filter:brightness(.35) blur(.4px)`，用 locked 台詞，武器欄灰底寫「還差 N 塊」，沒有「去提出申請」鈕。
- **採齊**：BOSS 套 `bossWake` 亮起，換 ready 台詞，武器欄從灰變亮（「可以鑄成」→「鑄得出來了」），右邊出現 **「去提出申請」** 連到 `SUB`。
- **老師還沒把礦脈開完**：BOSS 顯示 `？？？` 剪影，台詞換成「牠還沒完全現形——老師還有 N 塊沒開出來。」
- **第五層**：整列不顯示。改成一行字：「這一層沒有東西擋你。」

### 2. S-03 地圖 — 層底那一格　✅ 已做完，不用再做

前四層通道最後一格已經換成**那一層 BOSS 的像素圖**（`decorateBoard` 裡）：

- 未到那一層：全暗剪影 ＋ `？？？`。
- 現在這一層、未採齊：暗的（brightness .5）。
- 現在這一層、採齊：亮起 ＋ 金光 ＋ `bossFloat`。
- **已通過：牠消失**，格子換回打勾的「關卡」。
- 第五層維持「關」。
- 點牠＝點關卡格；被擋時 toast 先帶牠的台詞再講原因。

### 3. SUB 關卡送審頁 — 主場

最上面換成遭遇畫面：

```
┌──────────────────────────────────────────────┐
│              [BOSS 大圖 160px]                │
│                                              │
│   L2 · 千晶母                                 │
│   「你停手了。那才是難的部分。」                 │
│                                              │
│   護甲 ●●●●●●  6 / 6 已剝落                   │
│   ─────────────────────────────────           │
│   這 6 塊可以鑄成                              │
│   [切割器] 地層切割器                           │
│           切開擋路的東西——一刀斬斷長不完的那些。 │
│                                              │
│   但你還鑄不了。要先跟他說明。                   │
└──────────────────────────────────────────────┘
```

下面接現有三格，前面加一行小字：**「你要跟他說明三件事，他才會准你鑄。」題目文字不動**。
送出鈕：`送出關卡審核` → **`提出申請`**（擋住時仍是 `還不能送交關卡`）。

**第五層**：不畫 BOSS。改成——

> 這一層沒有東西擋你。
> 這裡只有一塊礦石，而且名字是你自己取的。
> 他沒有給你清單——要交什麼、什麼叫做完成，你自己說。
> 鑄出來的核心共振器不顯示任何讀數，你得自己聽它的音高。

### 4. 新跳頁 — 擊退（接在現有「拿道具與寶物」那一頁**之前**）

- kicker `BOSS DOWN`
- 標題 `擊退了霧面者`
- 大圖 BOSS ＋ `bossDefeat`
- rows：`鑄成 ○○ / 用了 N 塊礦石 / 停留 N 週`
- quote：老師的關卡審核理由，label「他放你過去的理由」
- lines：`你採的 N 塊礦石鑄成了○○，○○剋牠。` ＋ 那一層的 `hard` 原文 ＋ 「你剛剛做的就是這件事。」
- 按鈕 `知道了`（單純關掉，跟其他跳頁一致）

第五層不出這一頁，直接進現有的道具寶物頁。

### 5. 新跳頁 — 被擋下（關卡被退回）

- kicker `BLOCKED`
- 標題 `千晶母擋下來了`
- 大圖 BOSS ＋ `shake`（沿用現有 keyframe）
- quote：老師的退回理由，label「牠說」
- lines：那一隻的 blocked 台詞 ＋「礦石還在你手上，只是還鑄不成。三格可以重寫，重送次數不限。」
- 按鈕 `知道了`

> 逐項確認被退回的跳頁**不改**，那不是 BOSS。

### 6. 老師端 T-07 — 只加兩行字

- 標題下：`這一組正在申請鑄造「地層切割器」，用來對付千晶母` ＋ 小圖 ＋ `他們採齊了 6 塊`
- 通過鈕說明加一句：「你准了——礦石鑄成道具，牠讓開路。」
- 退回鈕說明加一句：「牠會擋下來——你寫的理由就是牠說的話。」

**老師端不加任何遊戲化按鈕。** 老師的介面維持專業感。

---

## 五、動態

`gas/Styles.html` 新增三組（現有八組不動）：

```css
@keyframes bossFloat{0%,100%{transform:translateY(0)}50%{transform:translateY(-4px)}}
@keyframes bossWake{0%{opacity:.3;filter:brightness(.35) blur(1px);transform:scale(.94)}
                    60%{opacity:1;filter:brightness(1.25) blur(0);transform:scale(1.04)}
                    100%{opacity:1;filter:none;transform:scale(1)}}
@keyframes bossDefeat{0%{opacity:1;transform:translateY(0) scale(1)}
                      35%{opacity:1;transform:translateY(-6px) scale(1.05)}
                      100%{opacity:.15;transform:translateY(26px) scale(.9)}}
```

各生物的專屬效果**用現有 keyframes 疊上去**，不必新增：

| 生物 | 效果 | 做法 |
|---|---|---|
| 霧面者 | 腳邊的霧飄動 | 底部 `~` 的 rect 套 `seep`，各自不同 delay |
| 千晶母 | 外圍碎晶閃爍增生 | 所有 `o` 的 rect 套 `facet`，delay 依 x 遞增 |
| 擬聲者 | 殘影錯位漂移 | `o` 的殘影套 `echo`，delay 差 .4s |
| 漲潮者 | 岩漿起伏 ＋ 火花上升 | 底部 `~` 套 `boil`，`*` 套 `ember` |

---

## 六、實作方式

### 圖怎麼來

**不要外部圖檔。** 用 inline SVG 的 `<rect>` 畫，跟現有五層場景 SVG 同一套做法（`shape-rendering="crispEdges"`）。

像素資料在 **`bosses/bosses.json`**，24×24 字元陣列：

```
. 透明   # 主體   o 次要／殘影／碎晶   * 高光   ~ 環境（霧、岩漿）
```

每隻帶一個 `hue` 對照表（四個字元 → 四個色碼）。

`bosses/gen.js` 的 `toSvg()` 已經是可用的實作（逐列掃描、合併同色成 rect）——**直接搬進 `gas/Live.html` 改成前端版本**，加一個 `opts.anim` 參數讓不同字元套不同 animation。

`bosses/*.svg` 只是靜態預覽，**不要**當執行期資產。

### 資料

寫在 `gas/Live.html` 的覆寫層，**不要改 `Logic.html`**：

```js
var BOSSES = {
  1: { id:'boss-L1-mianzhe', name:'霧面者', hue:{…}, px:[…],
       lineLocked:'你連我在哪都說不出來。',
       lineReady:'現在你看得到我了。說說看你走過哪裡。',
       lineBlocked:'牠沒有動。是你還沒說清楚。',
       anim:{ '~':'seep' } },
  2: { … anim:{ o:'facet' } },
  3: { … anim:{ o:'echo' } },
  4: { … anim:{ '~':'boil', '*':'ember' } }
  // 5 沒有
};
```

### 狀態

**後端一個欄位都不用加。** 全部由現有的 `layerState(L, n)` 推導：

```js
function bossState(L, layer) {
  var st = layerState(L, layer);
  return {
    boss:      BOSSES[layer] || null,        // L5 是 null
    forge:     forgeOf(L, layer),            // 這一層的礦石鑄的，L5 也有（只是沒對手）
    locked:    !st.complete,                 // 沒採齊 → 沉睡
    unopened:  st.unopenedN,                 // 老師沒開完 → ？？？
    armor:     st.veinN,
    stripped:  st.gotN,
    defeated:  st.cleared,
    fighting:  !!L.state.gateSubmitted && !st.cleared
  };
}
```

### 檔案

| 檔案 | 改什麼 |
|---|---|
| `gas/Styles.html` | 加三組 keyframes |
| `gas/Live.html` | `BOSSES`、`forgeOf()`、`bossSvg()`、`bossState()`、`decorateBoss()`；接進 S-02 / S-03 / SUB / 兩個跳頁 / T-07 |
| `patch-tpl.js` | 新 patch：S-02 頂端那一列、SUB 遭遇區、地圖層底換圖、T-07 兩行字 |
| `bosses/` | 素材來源，已備好 |

---

## 七、文案原則

沿用整個作品的語氣——**短句、不裝可愛、不用驚嘆號**。

BOSS 是「難處」的化身，不是反派。牠不恨你，牠只是站在那裡，因為那件事本來就難。

擊退之後不要寫「太棒了」「恭喜」。寫的是**你剛剛做到的那件事本身**：

> 難的是採不完，不知道什麼時候該收。
> 你剛剛做的就是這件事。

---

## 八、驗收

1. 老師還沒開完礦脈 → S-02 的 BOSS 是 `？？？`，說「牠還沒完全現形」。
2. 採 3/6 → BOSS 暗的、護甲亮 3 格、locked 台詞、武器欄寫「還差 3 塊」、沒有「去提出申請」鈕。
3. 採齊 6/6 → BOSS 用 `bossWake` 亮起、ready 台詞、武器欄轉亮、出現「去提出申請」連到 SUB。
4. **武器＝那一層自己的道具**：L1 礦脈羅盤、L2 地層切割器、L3 壓力量規、L4 熱流導航儀——圖與名稱都要對，**不可以出現上一層的道具**。
5. SUB 最上面是遭遇畫面，寫著「這 N 塊可以鑄成○○／但你還鑄不了」，送出鈕寫「提出申請」。
6. **第五層完全沒有 BOSS**：S-02 那一列不出現、地圖層底維持「關」、SUB 顯示「這一層沒有東西擋你」。
7. 老師退回關卡 → 學生看到「○○擋下來了」，quote 是老師的理由。
8. 老師通過 → 先看到「擊退了○○」，關掉之後才是道具與寶物那一頁。
9. 地圖前四層層底是 BOSS 縮圖；已過的打叉，未到的只有輪廓。
10. T-07 標「這一組正在挑戰「○○」」＋「他們手上有：○○」。
11. **演練模式十一步全部要有**（演練跑同一套 live 邏輯，理論上自動具備，但要驗）。
12. 三端掃過去零 console 錯誤、沒有 `undefined`。
